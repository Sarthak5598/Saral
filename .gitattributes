# Normalize all text files to LF in the repo, regardless of the host OS or
# core.autocrlf. This stops git from rewriting line endings on every checkout,
# which was causing every source file to look "modified" (touched mtime, no
# real content change) after simple operations like status or checkout.
* text=auto eol=lf

*.ps1 text eol=crlf
